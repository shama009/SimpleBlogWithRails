<h1>Blog posts</h1>
<% @posts.each do |post| %>
<div class ="card bg-faded">
<div class="card card-block">
        <h3><%= post.title %></h3>
        <p><%= post.body %></p>
        <%= link_to "Read More", post_path(post), :class=> 'btn btn-light'%>
        </div>
</div>
<% end %>